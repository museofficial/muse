services:
  muse:
    image: ghcr.io/asusplayer/muse:latest
    restart: always
    volumes:
      - ./data:/data
    environment:
      - DISCORD_TOKEN=
      - YOUTUBE_API_KEY=
      - SPOTIFY_CLIENT_ID=
      - SPOTIFY_CLIENT_SECRET=

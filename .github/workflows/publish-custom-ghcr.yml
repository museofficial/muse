name: Publish custom Docker image

on:
  workflow_dispatch:
  push:
    branches:
      - master
      - codex/personalize-muse-bot

env:
  REGISTRY_IMAGE: ghcr.io/iamnoodlex/muse

jobs:
  publish:
    name: Build and publish custom image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v6

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Get current time
        id: current-time
        shell: bash
        run: echo "time=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_OUTPUT"

      - name: Build Docker tags
        id: docker-tags
        shell: bash
        run: |
          safe_ref="$(echo "${GITHUB_REF_NAME}" | tr '/[:upper:]' '-[:lower:]' | sed 's/[^a-z0-9_.-]/-/g')"
          {
            echo "safe_ref=${safe_ref}"
            echo "short_sha=${GITHUB_SHA::12}"
          } >> "$GITHUB_OUTPUT"

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          platforms: linux/amd64,linux/arm64
          tags: |
            ${{ env.REGISTRY_IMAGE }}:custom
            ${{ env.REGISTRY_IMAGE }}:${{ steps.docker-tags.outputs.safe_ref }}
            ${{ env.REGISTRY_IMAGE }}:${{ steps.docker-tags.outputs.short_sha }}
          build-args: |
            COMMIT_HASH=${{ github.sha }}
            BUILD_DATE=${{ steps.current-time.outputs.time }}
